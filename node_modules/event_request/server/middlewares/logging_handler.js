'use strict';

// Dependencies
const Logger	= require( './loggers/logger' );

/**
 * @brief	Logging Handler that creates and holds all the possible loggers
 *
 * @TODO	IMPLEMENT
 *
 * @return	void
 */
class LoggingHandler
{
	constructor( options = {} )
	{
		this.options	= options;

		this.sanitizeConfig()
	}

	/**
	 * @brief	Sanitizes the config of the logging handler
	 *
	 * @return	void
	 */
	sanitizeConfig()
	{
	}
}

module.exports	= {
	LoggingHandler	: LoggingHandler,
	Logger			: Logger,
};
